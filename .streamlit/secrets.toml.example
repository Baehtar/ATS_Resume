# Copy this file to .streamlit/secrets.toml and replace values with your provider details.

[openai]
key = "sk-REPLACE_ME"
# Optional: custom OpenAI-compatible base URL for providers like Deepseeks.
# base_url = "https://api.deepseeks.ai/v1"
# Optional: custom model name if your provider uses a different model identifier.
# model = "gpt-3.5-turbo"

# Example Supabase values (if used by the app)
#[supabase]
#url = "https://xyzcompany.supabase.co"
#key = "public-anon-key"
